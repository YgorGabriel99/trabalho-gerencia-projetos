export class Player {
    public id: number;
    public name: string;
    
    constructor(data:Player) {
        {
            this.id = data.id,
            this.name = data.name
        }

    }
}

